# 提示词优化替换方案 — 技术评估与实施规划

> 日期：2026-04-22
> 状态：待实施

## 一、目标

将当前硬编码的 TS prompt 构建链替换为 AI 驱动的图像提示词优化器（image-prompt-optimizer），提升生成背景图的质量与多样性。

## 二、当前架构

### prompt 构建链（将被替换）

```
用户描述 "做一个春节主题"
  ↓
theme-intent-parser.ts    → 解析意图（category/subCategory/styleHints/toneHints/colorHints）
  ↓
theme-scene-planner.ts    → 根据 category + subCategory 选择固定场景模板，生成 sceneSentence + styleKeywords
  ↓
theme-prompt-director.ts  → 拼接 HARD_NEGATIVES + COMPOSITION_PREFIX + sceneSentence + styleKeywords
  ↓
executor.ts               → buildPromptWithPreferredHue() 追加主色指令
  ↓
finalPrompt (string)      → 传入 generateImage()
```

### 涉及文件

| 文件 | 职责 | 代码量 |
|------|------|--------|
| `web/src/tools/theme-intent-parser.ts` | 意图解析（6类分类 + festival/nature subCategory） | 154 行 |
| `web/src/tools/theme-scene-planner.ts` | 场景规划（intent → scenePlan + 偏好回注） | ~110 行 |
| `web/src/tools/theme-prompt-director.ts` | Prompt 组装（HARD_NEGATIVES + COMPOSITION_PREFIX） | 29 行 |
| `web/src/tools/executor.ts` L433-462 | prompt 构建编排 + 主色注入 | ~30 行 |
| `web/src/tools/theme-plan-checker.ts` | 场景计划校验 | 被调用 |
| `web/src/tools/theme-regeneration-director.ts` | 反馈重建场景 | 被调用 |

### 当前 prompt 输出示例

```
no text, no watermark, no UI elements, no buttons, no interface, no signature, no realistic frontal faces, silhouettes only if people appear.
A left-anchored scene with one clear focal subject in the foreground, supporting details in the midground, atmospheric depth in the background, warm bright daylight, the right side gently fading into open soft space.
red lanterns and golden tassels hanging close in the foreground, a celebratory archway with spring couplets in the midground, distant fireworks and warm sky glow in the background, joyful gathering silhouettes.
highly detailed, professional lighting, clean composition, refined color harmony.
Use a dominant festive red palette. Red must be the unmistakable primary visual color, with other accents only supporting it.
```

## 三、新方案核心特性

### 5 种路由模式

| 模式 | 触发条件 | 适用场景 |
|------|----------|----------|
| 通用增强 | 默认 | 大多数描述 |
| 中式美学 | 水墨/国潮/传统/节日等关键词 | 春节、中秋等节日主题 |
| 摄影质感 | 写实/胶片/电影感等关键词 | 企业、自然主题 |
| JSON 编排 | 输入已是 JSON 或要求 JSON 输出 | 结构化场景 |
| 奇幻视觉 | 奇幻/超现实等关键词 | 创意实验 |

### 关键增强点（相对当前方案）

1. **5 模式智能路由** — 替代当前固定模板
2. **双语输出** — 中文描述 + 英文提示词
3. **文字渲染规范** — 字体/位置/材质/排版（当前 `no text`）
4. **构图与视觉平衡** — 摄影构图语言替代机械空间指令
5. **输出后调整** — 多轮迭代优化
6. **模型适配层** — 可对接不同生图模型

## 四、架构分界线

`executor.ts` 的 `generate_theme_pipeline` 内部有清晰的两段结构：

```
┌─────────────────────────────────────────────────┐
│  上半段：prompt 构建（要替换的部分）                │
│  executor.ts L433-462                           │
│  parseThemeIntent → buildThemeScenePlan →        │
│  buildDirectedPrompt → buildPromptWithPreferredHue│
│  输出: finalPrompt (string)                      │
├────────────────── 断点 ──────────────────────────┤
│  L467: generateImage(finalPrompt)               │
│  → 返回 imgResult.url                           │
├────────────────── 断点 ──────────────────────────┤
│  下半段：颜色管道（完全不动）                       │
│  executor.ts L474-586                           │
│  analyzeImageAsync → pickBestThemeCandidate →    │
│  deriveColorsFromPrimary → validateColorScheme → │
│  updateColors → applyThemeImages                 │
└─────────────────────────────────────────────────┘
```

### 颜色管道函数清单 — 不受 prompt 改动影响

| 函数 | 所在文件 | 依赖来源 |
|------|---------|---------|
| `analyzeImageAsync(url)` | executor.ts | 生成图的 URL |
| `pickBestThemeCandidate(colors)` | executor.ts | 提取的像素色值 |
| `deriveColorsFromPrimary(hex)` | color-utils.ts | 主色 hex |
| `validateColorScheme(colors)` | contrast-validator.ts | CSS 变量集 |
| `updateColors(colors)` | executor.ts | CSS 变量集 |
| `applyThemeImages(template, url)` | executor.ts | 图片 URL |
| `buildThemeGenerationReport()` | color-utils.ts | 主色 hex |
| `reviewGeneratedImage()` | theme-image-reviewer.ts | 上述结果汇总 |

这些函数全部基于**从生成图片中提取的像素颜色**工作，与 prompt 构建链无交叉引用。

## 五、风险与注意事项

### 1. MiniMax image-01 文字渲染能力 — 高风险

当前 `HARD_NEGATIVES` 明确写 `no text`，因为 MiniMax image-01 文字渲染能力有限。新方案的文字排版规则（瘦金体、烫金、竖排等）可能无法被该模型有效执行。

**建议**：OA 主题背景图保持 `no text`，文字渲染规则仅在其他场景启用。

### 2. prompt 长度 — 中风险

新方案输出 3-6 句详细描述 + 构图规则 + 文字规范，可能远超当前 ~500 字符。`chat-client.ts` 已有 1500 字符截断保护，但 MiniMax 对长 prompt 的理解质量可能下降。

**建议**：监控生成效果，必要时精简或分段。

### 3. 双语输出与现有链路 — 中风险

现有 `generateImage()` 只发送单个 prompt。新方案要求中英双语输出。

**建议**：只取英文版本传给 MiniMax image-01，中文版本仅用于 UI 展示（如有需要）。

### 4. OA 主题场景适配

新方案是通用图像提示词优化器，但本项目生成的是 OA 系统 UI 背景，核心需求：
- 左侧主体、右侧虚化（适配 UI 遮罩）
- 干净、无干扰元素
- 配色可提取、对比度合格

**建议**：保留 `COMPOSITION_PREFIX`（左锚定构图）和核心 HARD_NEGATIVES，作为 OA 适配层叠加到新方案输出之上。

## 六、实施方向建议

### 推荐路径：新增 tool 调用链

在 `generate_theme_pipeline` 中，将 prompt 构建部分替换为：

```
1. 用户描述 → AI 聊天模型（M2.7）按优化器规则生成优化后的 prompt
2. 优化后的 prompt + OA 适配层（左锚定构图 + no text + 主色指令）
3. 最终 prompt → generateImage() → 颜色管道（不变）
```

### 需要新增/修改的文件

| 文件 | 动作 | 说明 |
|------|------|------|
| `web/src/tools/prompt-optimizer.ts` | **新增** | 新的 prompt 优化器，集成 5 模式路由规则 |
| `web/src/tools/executor.ts` L433-462 | **修改** | 调用新的 prompt 优化器替代旧的构建链 |
| `web/src/agent/system-prompt.ts` | **修改** | 可能需要注入优化器路由规则 |
| `web/src/agent/chat-client.ts` | **修改** | 可能需要新增优化器专用 API 调用 |
| `web/src/tools/theme-intent-parser.ts` | **保留** | 意图解析仍可复用 |
| `web/src/tools/theme-scene-planner.ts` | **精简或删除** | 固定场景模板被优化器替代 |
| `web/src/tools/theme-prompt-director.ts` | **精简或删除** | 被 prompt-optimizer 替代 |

### 颜色管道文件 — 全部不动

- `web/src/theme/color-utils.ts`
- `web/src/tools/contrast-validator.ts`
- `web/src/tools/theme-image-reviewer.ts`
- `web/src/tools/executor.ts` L474-586（颜色管道部分）

## 七、新方案完整内容

以下为用户提供的 image-prompt-optimizer 完整规范，作为实施参考：

---

### 路由规则

按以下优先级判断模式：

- **0. 风格/媒介关键词优先**：当用户输入中明确包含风格或媒介关键词（摄影/胶片/水墨/油画等）时，以该关键词对应的模式为主模式
- **1.** 用户明确要求 JSON 输出 → JSON 编排
- **2.** 输入已是结构化 JSON → JSON 编排
- **3.** 中式美学关键词（水墨/国潮/传统/诗词/留白/工笔/青绿/宣纸/节气/生肖/书法/印章/中国红/青花瓷等） → 中式美学
- **4.** 摄影关键词（写实/胶片/电影感/写真/摄影/镜头/景深等） → 摄影质感
- **5.** 奇幻关键词（奇幻/超现实/颠覆/炼金等） → 奇幻视觉
- **6.** 否则 → 通用增强

### 输出总约束

- 禁止自我介绍、解释能力、添加前缀
- 禁止使用技术参数（采样/步数/seed/光圈/ISO/焦段等）
- 禁止使用权重语法或负面清单
- 禁止点名在世艺术家或受保护 IP
- 仅输出提示词本体
- 每个关键名词配 2-3 个精准修饰词
- 保持用户原始创意意图不变
- 默认适配 16:9 横版画幅

### 构图与视觉平衡策略（UI 适配）

- 禁止使用"留白"、"预留空间"等机械指令
- 必须使用景深与光影过渡填充右侧区域
- 视觉重心偏左，核心主体位于画面左侧或左中位置
- 推荐三分法构图、浅景深处理、对角线引导

### 文字生成与排版规范

- 逐字保留用户文案
- 主标题与辅助文案成组排版
- 字体描述需明确书法或字体风格
- 文字色彩从画面主色调中提取
- 禁止文字与背景处于同一明度层级

### 视觉风格词库

- 绘画媒介（9）：水彩、油画、素描、水墨、手绘、厚涂、线稿、涂鸦、拼贴
- 二维风格（16）：卡通、动漫、漫画、二次元、Q版、赛璐璐、绘本风、像素风等
- 3D/CG（8）：3D渲染、C4D、Blender、黏土风、潮玩3D等
- 写实/摄影感（5）：写实、胶片、电影感、摄影、写真
- 历史/艺术流派（5）：哥特、巴洛克、洛可可、复古、蒸汽波
- 地域风格（8）：中国风、国潮、日式、韩式、美式、欧式、北欧、工业风
- 未来/幻想（4）：赛博朋克、科幻、蒸汽朋克、虚拟现实
- 专业领域风格（5）：原画、概念设计、游戏美术、盲盒、插画
- 当代艺术（4）：故障艺术、极简光影、新丑风、弥散光效

### 模式详解

- **模式 A 通用增强**：按主体/环境/光线/色彩/氛围/构图 6 维度组织 3-6 句
- **模式 B 中式美学**：注入水墨/工笔/青绿山水/传统纹样/诗性表达/中式色彩
- **模式 C 摄影质感**：明确主被摄体/景深/光质/胶片质感
- **模式 D JSON 编排**：严格 JSON 对象，字段名与值均中文
- **模式 E 奇幻视觉**：解构→重构→跃迁→保留，自由创作

### 双语输出策略

- 默认启用双语（中文描述 + 英文提示词）
- 英文版本受 Token 上限约束，超限时优先保留核心视觉元素
- JSON 模式不启用双语

### 输出后调整

- 支持从 7 个维度（主体/构图/光线/色彩/风格/文字/整体）增量调整
- 基于最近一轮输出为基准进行增量修改
- 双语模式下中英文同步更新

---

## 八、今日已完成的清理工作

已从项目中移除 `generate_theme_previews`（多方案预览）模式：

| 文件 | 删除内容 |
|------|---------|
| `web/src/tools/executor.ts` | `generate_theme_previews` case、`apply_selected_theme` case、`ThemePreview` 类型 |
| `web/src/chat-manager.ts` | `latestThemePreviews` 变量、`__selectThemePreview` 全局函数、多图 UI 渲染分支 |
| `web/src/agent/tool-call-utils.ts` | `detectThemeSelection` 函数、`latestThemePreviews` 参数 |
| `web/src/tools/theme-scene-planner.ts` | `buildExploratoryScenePlans`、`ExploratoryDirection`、`DIRECTION_TEMPLATES` 等 |
| `docs/internal/AGENTS.md` | 3 处文档更新 |

当前系统只保留 `generate_theme_pipeline` 单图生成模式。

## 九、环境信息

- 后端服务：`cd server && npm start`（端口 3001）
- 前端服务：`cd web && npm run dev`（端口 5173）
- `.env` 文件位置：项目根目录 + `server/` 目录（两处都需要）
- TypeScript 编译：`cd web && ./node_modules/.bin/tsc --noEmit`（忽略 `sharp` 类型错误）
