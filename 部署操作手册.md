# Theme Studio 部署操作手册

## 1. 适用范围

本文档用于将 Theme Studio 作为**线上 B 端服务**部署到 Linux 服务器运行。

当前系统包含：

- Node.js 服务端：提供鉴权、AI 代理、对话、导出任务接口
- Web 前端：由 Vite 构建为静态资源
- Python 脚本链路：负责导出素材准备、截图、打包与校验

## 2. 部署前提

服务器至少需要具备：

- Linux x86_64
- Node.js 20+
- npm 10+
- PM2（用于生产进程托管）
- Python 3.10+
- pip
- 可访问 EKP 与大模型 API 的外网/专线网络

如需使用“在线导出打包”能力，还需要：

- Pillow
- Playwright 依赖浏览器环境

## 3. 必备环境变量

在服务器项目根目录创建 `.env`：

```env
PORT=3001
ADMIN_PASSWORD=请替换为强密码

EKP_BASE_URL=https://myekp.landray.com.cn
EKP_SSO_USER=third_ai_01
EKP_SSO_PASS=你的EKP密码
EKP_TOKEN_RESOLVE_PATH=/sys/authentication/sso/loginService_rest/getTokenLoginName

SCREENSHOT_BASE_URL=https://你的正式域名
```

说明：

- `ADMIN_PASSWORD`：后台管理登录口令，不能为空
- `EKP_BASE_URL` / `EKP_SSO_USER` / `EKP_SSO_PASS`：企业 SSO 必填
- `SCREENSHOT_BASE_URL`：服务端在导出时需要访问前端页面截图，必须配置成正式域名
- `ENABLE_DEV_AUTH`：生产环境不要配置

## 4. 首次部署步骤

### 4.1 上传代码

```bash
git clone <你的仓库地址>
cd "Topic Automation"
```

### 4.2 安装依赖

```bash
npm install
npm install -g pm2
cd server && npm install && cd ..
cd web && npm install && cd ..
python3 -m pip install pillow
npx playwright install chromium
```

如果是全新 Linux 服务器，建议同时安装 Playwright 所需系统依赖：

```bash
npx playwright install --with-deps chromium
```

### 4.3 构建

```bash
npm run build
```

构建结果：

- `server/dist/`：服务端编译产物
- `web/dist/`：前端静态资源

### 4.4 启动服务（PM2）

```bash
npm run start
npm run status
```

查看日志：

```bash
npm run logs
```

重启：

```bash
npm run restart
```

停止：

```bash
npm run stop
```

## 5. Nginx 反向代理示例

建议同域部署，让浏览器自动携带企业 Cookie `LR_myekp`。

仓库已提供模板文件：`deploy/nginx/theme-studio.conf.example`

```nginx
server {
    listen 80;
    server_name your-domain.example.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.example.com;

    ssl_certificate     /etc/ssl/your/fullchain.pem;
    ssl_certificate_key /etc/ssl/your/privkey.pem;

    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

部署时建议流程：

```bash
sudo cp deploy/nginx/theme-studio.conf.example /etc/nginx/conf.d/theme-studio.conf
sudo nginx -t
sudo systemctl reload nginx
```

## 6. PM2 / systemd 启动模板

### 6.1 PM2 启动

仓库已提供：`ecosystem.config.cjs`

```bash
npm run start
npm run status
pm2 save
pm2 startup
```

### 6.2 systemd 启动

如果你们不用 PM2，可以使用模板：`deploy/systemd/theme-studio.service`

```bash
sudo cp deploy/systemd/theme-studio.service /etc/systemd/system/theme-studio.service
sudo systemctl daemon-reload
sudo systemctl enable theme-studio
sudo systemctl start theme-studio
sudo systemctl status theme-studio
```

注意：

- 把 `WorkingDirectory=/srv/theme-studio` 改成你们服务器真实路径
- 确保 `User` 对 `data/`、`logs/`、`output/service-jobs/` 有写权限

## 7. 部署后验证清单

### 7.1 健康检查

```bash
curl http://127.0.0.1:3001/api/health
```

预期返回：

```json
{"status":"ok","db":"connected"}
```

### 7.2 管理后台

- 打开：`https://你的域名/admin`
- 使用 `ADMIN_PASSWORD` 登录
- 配置 Chat / Image 模型参数

### 7.3 企业 SSO 验证

- 在企业门户已登录状态下访问前台
- 浏览器应自动带上 `LR_myekp`
- `/api/auth/me` 返回当前企业用户名

### 7.4 导出链路验证

- 新建主题项目
- 上传图片或生成主题
- 点击导出
- 检查 `output/service-jobs/` 下是否生成任务目录与产物

### 7.5 一键生产检查脚本

仓库已提供：`scripts/check-production.sh`

```bash
bash scripts/check-production.sh
```

脚本会检查：

- `.env` 是否存在
- 必要环境变量是否齐全
- `server/dist/index.js` 是否已构建
- TypeScript 类型检查
- `/api/health` 与 `/admin` 可访问性
- `SCREENSHOT_BASE_URL` 可访问性
- `data/`、`logs/`、`output/service-jobs/` 是否可写

## 8. 数据目录说明

- `data/`：SQLite 数据、备份
- `logs/`：服务日志
- `output/service-jobs/`：在线导出任务产物

建议把这三个目录挂载到持久化磁盘。

## 9. Docker 部署

构建镜像：

```bash
docker build -t theme-studio:latest .
```

当前 `Dockerfile` 已在镜像内预装：

- Python 3 + Pillow
- `web/scripts/screenshot.ts` 在线导出截图脚本
- `web` 运行依赖（含 `vite`、`tsx`、`playwright`）
- Chromium 与 Playwright 所需 Linux 依赖

首次构建时间会比普通 Node 服务更长，属于正常现象。

运行：

```bash
docker run -d \
  --name theme-studio \
  -p 3001:3001 \
  --env-file .env \
  -v $(pwd)/data:/app/data \
  -v $(pwd)/logs:/app/logs \
  -v $(pwd)/output:/app/output \
  theme-studio:latest
```

注意：

- 仍然必须通过 `.env` 提供 `ADMIN_PASSWORD`、`EKP_*`、`SCREENSHOT_BASE_URL`
- `SCREENSHOT_BASE_URL` 需要指向容器对外可访问的正式域名，不能写容器内的 `127.0.0.1`
- 在线导出产物位于挂载后的 `output/service-jobs/`

## 10. 常见问题

### 10.1 启动时报 `ADMIN_PASSWORD is required`

说明 `.env` 未配置，补齐后重启。

### 10.2 前台一直跳登录

说明：

- 浏览器没有带上 `LR_myekp`
- 或 `EKP_BASE_URL / EKP_SSO_USER / EKP_SSO_PASS` 配置错误

### 10.3 导出失败

优先检查：

- `SCREENSHOT_BASE_URL` 是否为外部可访问域名
- Playwright/浏览器运行环境是否正常
- Python Pillow 是否已安装

## 11. 回滚

若新版本异常：

```bash
git checkout <上一个稳定版本tag或commit>
npm install
cd server && npm install && cd ..
cd web && npm install && cd ..
npm run build
npm run restart
```

## 12. 生产建议

- 所有密钥只放 `.env` 或密钥管理系统，不写入仓库
- Nginx 必须开启 HTTPS
- `ENABLE_DEV_AUTH` 不要在生产配置
- 建议每日备份 `data/` 与 `output/service-jobs/`
- 上线前先执行 `bash scripts/check-production.sh`
