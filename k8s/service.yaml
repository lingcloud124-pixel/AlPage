apiVersion: v1
kind: Service
metadata:
  name: theme-studio-svc
  namespace: theme-studio
  labels:
    app: theme-studio
spec:
  type: ClusterIP
  selector:
    app: theme-studio
  ports:
    - name: http
      port: 3001
      targetPort: 3001
      protocol: TCP
