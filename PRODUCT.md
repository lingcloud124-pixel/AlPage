# Theme Studio — 产品定义文档

> **版本**: v5.0 | **更新日期**: 2026-04-12
> **本文档是产品的单一事实来源，所有迭代围绕本文档进行。**

---

## 一、产品定位

**一句话**：用户通过对话生成和管理 OA 主题，实时看到效果图，满意后按需打包。

---

## 二、用户操作流程

### 2.1 首次进入（直接对话）

用户打开应用后直接进入对话界面，无首页或弹窗：
- AI 发送欢迎消息，展示几个热门预设推荐
- 用户可直接描述需求或点击推荐卡片开始
- 所有历史项目在左侧侧边栏，默认收起

### 2.2 对话生成配色

用户在对话面板输入需求（如 "做一个篮球主题"）：
- AI 流式回复，展示推理过程
- 自动调用工具：update_colors / analyze_image / parse_pen
- 配色生成后，右侧预览实时更新
- AI 同时推荐相关预设配色方案

### 2.3 预设推荐卡片

AI 回复中包含 `[preset:xxx]` 标签时自动渲染为可点击卡片：
- 35 个预设配色方案（节日、场景、行业等）
- 卡片显示色块和名称
- 点击卡片直接应用配色，实时更新预览
- 自动判断 light-ui / dark-ui

### 2.4 模板预览切换

右侧预览区支持 Tab 切换：
- 登录页：密码/短信/扫码三种登录方式，完整交互
- 工作台：header + sidebar + 5 个 widget（待办/新闻/公告/日程/快捷入口）
- 所有颜色通过 21 个 CSS 变量实时同步

### 2.5 打包导出

点击打包按钮：
- 弹窗选择 15 个产品包（MK/V12/V13.5/V14/V17 等）
- 生成 YAML + JSON 配置文件
- 下载主题包到本地

---

## 三、界面架构

### 3.1 三列布局

应用采用豆包风格的三列布局：
- 第一列：项目侧边栏（260px，深色 #1a1a2e，默认收起）
- 第二列：AI 对话面板（可拖拽宽度，流式输出）
- 第三列：模板预览区（自适应剩余宽度）

### 3.2 项目侧边栏

左侧项目管理面板：
- 深色背景 (#1a1a2e)
- 顶部 "新建项目" 按钮
- 历史项目列表（时间倒序）
- 默认收起，点击汉堡按钮切换显示/隐藏
- 点击项目切换到该项目上下文

### 3.3 AI 对话面板

中央对话交互区（豆包风格）：
- 流式输出（streaming）
- Typing 三点动画 → streaming 光标闪烁
- 预设推荐卡片自动渲染
- AI 气泡：18px border-radius，渐变紫色头像
- 底部输入框 + 发送按钮

### 3.4 模板预览区

右侧实时预览展示：
- 顶部 Tab 切换：登录页 / 工作台
- 实时颜色更新（CSS 变量）
- 响应式布局
- 悬停状态和过渡动画

---

## 四、AI 能力

### 4.1 对话流程

1. 用户输入需求描述
2. AI 分析需求，生成配色方案
3. 流式输出推理过程
4. 自动调用工具应用配色
5. 右侧预览实时更新
6. 推荐相关预设配色方案

### 4.2 工具调用

AI 可调用以下工具：
- **update_colors**: 更新 CSS 变量颜色
- **analyze_image**: 分析图片提取主色调
- **parse_pen**: 解析 .pen 文件
- **save_colors**: 保存配色到 localStorage
- **load_colors**: 加载历史配色

### 4.3 跨项目偏好记忆

localStorage 持久化用户偏好：
- 自动从对话中提取用户偏好（行业、风格、色调、亮暗模式）
- 注入 system prompt，AI 不重复询问已知偏好
- 跟踪预设使用历史（最近 20 个）
- 跨项目共享用户偏好

### 4.4 预设推荐

AI 回复中 `[preset:xxx]` 标签处理：
- 自动渲染为可点击卡片
- 卡片显示色块和名称
- 点击直接应用配色
- 实时更新预览区

---

## 五、模板系统

### 5.1 登录页

自适应登录页模板：
- 密码登录、短信登录、扫码登录三种方式
- 完整的表单交互（focus、hover、error 状态）
- Logo、背景图、表单布局
- 响应式设计

### 5.2 工作台

自适应工作台模板：
- Header 区域（用户信息、搜索、通知）
- Sidebar 导航（可折叠、分组、图标）
- 5 个 widget：待办事项、新闻资讯、公告通知、日程安排、快捷入口
- 卡片式布局，完整交互态

### 5.3 页眉变体（8种）

Header 提供 8 种布局变体：
- default：默认页眉
- v16-default：V16 风格默认页眉
- simple：简约页眉
- simple-multitab：简约多标签页眉
- multitab：多标签页眉
- banner：横幅页眉
- classic：经典页眉
- menu：菜单页眉

### 5.4 侧边栏

导航侧边栏组件：
- 分组导航菜单
- 可折叠子菜单
- 图标 + 文字布局
- Hover 高亮效果
- 当前选中状态

### 5.5 CSS 变量体系

所有模板使用 21 个 CSS 变量：
- 主题色系（7 个）：primary-color、alter-color、透明度变体
- 文字色系（3 个）：标题、正文、辅助灰
- 背景色系（4 个）：body、header、sidebar、login
- 其他（7 个）：border、gradient、icon 等

---

## 六、预设配色

35 个预设配色方案：
- 节日主题：春节、国庆、圣诞节等
- 场景主题：篮球、星际探索等
- 行业主题：金融、医疗、教育等
- 每个预设包含完整的 21 个 CSS 变量
- 自动判断 light-ui / dark-ui
- 点击卡片直接应用配色

---

## 七、打包导出

15 个产品包支持：
- MK：主题包、登录包
- V12：主题包、登录包
- V13.5：主题包、3 个登录包（V13/V13.5/V13_5）
- V14~V16：主题包、3 个登录包（V14/V15/V16）
- V17：主题包、登录包

打包流程：
1. 选择要打包的产品（复选框）
2. 生成 YAML + JSON 配置文件
3. 下载所有选中的主题包

---

## 八、技术规格

### 技术栈

| 层 | 技术 |
|----|------|
| 前端框架 | 纯 HTML + CSS + TypeScript（无框架） |
| CSS 工具 | Tailwind CSS v4（仅全局引入） |
| 开发服务器 | Vite |
| 颜色系统 | CSS 变量（:root） |
| 存储 | localStorage |
| UI 语言 | 中文 |

### 核心特性

- **三列布局**：豆包风格，可拖拽宽度
- **流式输出**：typing 三点动画 + streaming 光标
- **预设推荐**：AI 回复中自动渲染卡片
- **跨项目记忆**：localStorage 持久化用户偏好
- **模板产品化**：hover 状态、过渡动画、交互细节
- **实时预览**：CSS 变量实时同步

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v5.0 | 2026-04-12 | 架构重构：三列布局（豆包风格）、跨项目偏好记忆、预设推荐卡片、AI 对话系统 |
| v4.1 | 2026-04-11 | 新增完整用户操作流程：首页 → 项目工作台 → 修改面板 → 打包弹窗 |
| v4.0 | 2026-04-11 | 架构重构：原生 HTML 模板替代 .pen 渲染引擎 |