# 规则整合与预览截图打包实施计划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 把历史整理的主题色规则、Pen 切图规则、页眉映射关系和打包分层思路正式沉淀进项目，并逐步落地到可复用的规则入口，支撑 HTML 预览与截图打包统一链路。

**Architecture:** 先把历史外部资料整理为项目内规则文档，再建立机器可读的规则注册表，最后让预览与导出都围绕同一套规则运行。短期内优先沉淀规则、校准映射、消除隐性硬编码，避免继续让外部文档与项目实现脱节。

**Tech Stack:** Markdown 文档、TypeScript、Vitest、现有 `src/utils/*` / `src/config/*` 配置层、Playwright 截图链路、Python 打包链路

---

### Task 1: 沉淀历史外部资料

**Files:**
- Create: `docs/外部资料整理-主题规则切图打包映射.md`
- Modify: `docs/README.md`

**Step 1: 整理外部资料中的高价值规则**

梳理以下来源中的核心结论：

- `Dark-UI-色值映射表.md`
- `Pen切图对照规范.md`
- `20260403/初稿文件/*`
- `20260403/关系对照表/*`

重点提炼：

- Dark-UI 登录页专用色
- Pen 节点 → 输出文件名 → 尺寸规则
- 页眉命名关系
- 打包层职责分层

**Step 2: 写入项目内规则整理文档**

在 `docs/外部资料整理-主题规则切图打包映射.md` 中明确：

- 关键规则
- 重要结论
- 当前冲突点
- 后续项目内沉淀方向

**Step 3: 更新 docs 导航**

把该文档加入 `docs/README.md`。

**Step 4: 验证文档落盘**

Run:

```bash
cd '/Users/gulingfei/Desktop/APP（vibe-coding）/Topic Automation' && ls docs
```

Expected:

- 新文档存在
- README 中已可访问

---

### Task 2: 建立机器可读规则注册表

**Files:**
- Create: `src/config/themeRuleRegistry.ts`
- Create: `tests/unit/ThemeRuleRegistry.test.ts`

**Step 1: 写 failing test**

测试至少覆盖：

- Dark-UI 登录页特殊色存在
- 登录背景导出规则存在
- Light/Dark 页眉基础导出规则存在
- 关键页眉关系映射存在

**Step 2: 运行测试确认失败**

Run:

```bash
npm run test:unit -- tests/unit/ThemeRuleRegistry.test.ts
```

Expected:

- FAIL
- 提示模块或导出不存在

**Step 3: 最小实现规则注册表**

在 `src/config/themeRuleRegistry.ts` 中导出：

- `DARK_UI_SPECIAL_COLORS`
- `PEN_EXPORT_RULES`
- `HEADER_TYPE_RELATIONS`

要求：

- 只沉淀最关键规则
- 不提前做“大而全平台化”
- 先保证命名清晰、可测试、可扩展

**Step 4: 重新运行测试**

Run:

```bash
npm run test:unit -- tests/unit/ThemeRuleRegistry.test.ts
```

Expected:

- PASS

---

### Task 3: 对齐现有 Pen 节点与导出映射

**Files:**
- Modify: `src/utils/penNodeMappings.ts`
- Modify: `tests/unit/ThemeRuleRegistry.test.ts`
- Add/Modify: `tests/unit/*`（如需补充针对性测试）

**Step 1: 对照当前实现与历史规则**

重点对照：

- login 背景节点
- header 60/90/130
- banner
- sideheader
- gradient 资源节点

**Step 2: 识别并标出冲突**

如果外部资料之间有冲突：

- 先写进注释或文档
- 不要悄悄强写进代码
- 以当前项目内可验证实现为准

**Step 3: 最小修正现有映射**

仅修正那些：

- 已有证据明确
- 能提升预览/导出一致性
- 不会引入大面积行为变化

**Step 4: 运行针对性测试**

Run:

```bash
npm run test:unit -- tests/unit/ThemeRuleRegistry.test.ts tests/unit/ThemeUpdater.test.ts tests/unit/ImageProcessor.test.ts
```

Expected:

- PASS

---

### Task 4: 把规则入口接入后续导出链路

**Files:**
- Modify: `scripts/generate-manifest.mjs`
- Modify: `src/utils/imageMappings.ts`
- Modify: `web/scripts/screenshot.ts`
- Test: `tests/unit/*`

**Step 1: 找到当前仍依赖隐式约定的地方**

例如：

- 文件名复用
- header 映射写死
- 特殊色散落

**Step 2: 最小接入新的规则入口**

原则：

- 能读取统一规则入口就不要继续手写魔法值
- 先改最关键路径，不一次性改完全部系统

**Step 3: 运行验证**

Run:

```bash
npm run test:unit
npm run build
cd web && npm run build
```

Expected:

- 全部通过

---

### Task 5: 文档收口与下一阶段交接

**Files:**
- Modify: `docs/README.md`
- Modify: `docs/实施路线图-开发任务拆分表.md`
- Modify: `docs/三阶段演进计划.md`

**Step 1: 把规则注册表纳入文档**

明确：

- 规则文档是认知层
- 规则注册表是执行层

**Step 2: 更新实施路线**

把“机器可读规则注册表”写入短期任务。

**Step 3: 完成阶段性说明**

记录：

- 已沉淀了哪些规则
- 哪些冲突仍待人工确认
- 下一阶段执行入口是什么

---

## 当前执行建议

优先顺序：

1. 先完成 Task 1（文档沉淀）
2. 立即完成 Task 2（规则注册表）
3. 再推进 Task 3（现有映射对齐）

这是当前最稳、最不容易返工的路线。

