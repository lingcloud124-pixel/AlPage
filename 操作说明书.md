# 主题自动化生成系统 - 操作指南

## 快速开始

### 第一次使用

1. **安装依赖**
   ```bash
   npm install
   ```

2. **安装 Skill（AI 辅助用）**
   ```bash
   bash scripts/install-skill.sh
   ```
   然后重启 OpenCode。

3. **告诉 OpenCode**
   ```
   这是主题自动化项目，我需要生成一个新主题
   ```

---

## 🆕 新建会话使用指南（重点！）

### 在新会话中生成新主题的正确步骤

当你打开一个**全新的对话窗口**时，按以下步骤操作：

**第 1 步：加载项目**
```
这是主题自动化项目，用 npm install 安装依赖
```

**第 2 步：触发 Skill**
```
使用 SKILL.md 生成一个新主题：{你的主题描述}
```
例如：
- "使用 SKILL.md 生成一个新主题：2026年清明节，绿色科技风格"
- "使用 SKILL.md 生成一个新主题：中秋节，金色温暖风格"

**第 3 步：确认初始报告**

Skill 加载后，AI 会向你报告：
```
✅ Skill 已加载。当前主题：清明节
📋 当前阶段：阶段 1/4（配色方案生成前）
⚠️ 待确认：模板类型（light-ui 或 dark-ui）
```

**检查报告中的信息是否正确：**
- 主题名是否准确？
- 模板类型是否符合预期？

如果不正确，立即纠正。如果正确，告诉 AI："**确认，开始阶段 1**"

**第 4 步：按阶段推进**

每次完成一个阶段，AI 会向你展示验证报告。**你必须检查后确认才能进入下一阶段：**

```
AI: ✅ 配色方案已从图片分析生成
你: 确认，进入下一步
    ↓
AI: ✅ 背景图已生成，pen 文件已更新
你: 在 Pencil 中检查确认
    ↓
AI: ✅ 素材导出完成，17个文件尺寸全部正确
你: 确认，进入打包
    ↓
AI: ✅ 打包完成：15/15 全部通过验证
```

**⚠️ 重要：**
- 如果任何阶段验证失败，告诉 AI "有问题，修复后再继续"
- Pencil 打开后必须手动确认，AI 不会自动继续
- 颜色必须由图片决定，不能凭空编造

---

## 日常使用

### 你只需要说一句话

| 你想做什么 | 告诉 OpenCode |
|-----------|--------------|
| 生成清明节主题 | "使用 SKILL.md 生成一个2026年清明节主题，绿色风格" |
| 生成中秋节主题 | "使用 SKILL.md 生成中秋节主题，金色风格" |
| 更新主题颜色 | "把这个主题的主色调改成红色" |
| 批量更新主题包 | "用新配色更新所有主题包" |
| 只换图片不换颜色 | "只更新主题包里的图片，不要改颜色" |

---

## 标准工作流程（4阶段）

```
1️⃣ 阶段 1: 配色方案生成（Color scheme from image analysis）
    ↓ [验证：配色规则]
2️⃣ 阶段 2: 背景图生成 + Pen 文件更新（Background generation + Pen update via update-pen-theme.py）
    ↓ [验证：截图确认]
3️⃣ 阶段 3: 素材导出 + 裁剪（Material export + cropping to correct sizes）
    ↓ [验证：尺寸校验]
4️⃣ 阶段 4: 批量打包（Batch packaging via theme_builder.py）
    ↓ [验证：15个包全部成功]
✅ 完成，在 output/{日期}-{主题名}/输出包/ 中取 zip 文件
```

**注意**: "你描述主题需求" 是使用前的准备工作，不属于正式阶段。

---

## 你需要提供的

| 类型 | 说明 |
|------|------|
| **主题描述** | 如"清明节、绿色科技风格" |
| **模板类型** | light-ui（浅色）或 dark-ui（深色），不清楚时告诉 AI。注意：Light-UI 是主要支持模式，Dark-UI 尚未完全实现 |
| **Pencil 设计确认** | AI 插入背景图后，你需要在 Pencil 中查看确认 |

---

## 取结果在哪里

完成后，到你的**最新日期文件夹**里取 zip 文件：

```
output/20260409-{主题名}/输出包/
├── 主题-MK-{主题名}.zip
├── 登录-MK-{主题名}.zip
├── 主题-V12-{主题名}.zip
├── 登录-V12-{主题名}.zip
└── ...
```

**图片素材**在这里（如果需要）：
```
output/20260409-{主题名}/素材包/
├── bg-login.jpg
├── header-banner.png
├── desktop.png
└── ...
```

---

## 目录结构

```
Topic Automation/
├── SKILL.md               # 技能文档（AI 使用）
├── 操作说明书.md           # 本文件
├── scripts/
│   ├── install-skill.sh   # Skill 安装
│   ├── theme_builder.py   # 统一打包工具（生成15个zip包）
│   ├── update-pen-theme.py # pen文件颜色/图片更新器
│   ├── verify-build.py    # 打包后验证工具
│   └── run-updater.mjs    # 批量打包（已弃用，旧工具链）
├── templates/             # 主题模板目录
│   └── Light-UI/Dark-UI   # 模板子目录
├── rules/                 # ⭐ 技术规则（AI 必须遵守）
│   ├── dark-ui-color-rules.md
│   ├── light-ui-color-rules.md
│   ├── image-generation-rules.md
│   └── export-rules.md
├── workflows/             # 流程文档（AI 必须遵守）
├── colors/                # 配色方案
│   └── cherry-blossom.json
├── designs/
│   ├── sources/
│   │   └── Light-UI-模板.pen
│   └── Topic-*.pen
├── assets/references/samples/
│   └── 主题样例包/         # 主题样例包（标准路径名，symlink to 样例包）
│       ├── 主题-*.zip     # 官方样例包（不要删除）
│       └── 登录-*.zip
└── output/
    ├── 20260409-武汉的樱花开了/
    │   ├── 素材包/         # 切图文件
    │   └── 输出包/         # 最终 zip
    └── history/            # 旧版本归档
```

**注意**: `src/` 目录包含旧的 TypeScript 代码（已弃用）。

---

## 清理指南

### 可以安全删除的

| 什么文件 | 说明 |
|---------|------|
| `output/{日期}-*/输出包/*.zip` | 生成的主题包，取走后可删除 |
| `designs/Topic-旧主题-*.pen` | 旧主题设计文件，确认不需要后可删除 |

### 核心文件（绝对不要删除）

- `designs/sources/Light-UI-模板.pen` - 设计模板
- `designs/sources/Dark-UI-模板.pen` - Dark 模板
- `assets/references/samples/主题样例包/` - 官方主题样例包
- `colors/*.json` - 配色方案
- `src/` - 源代码
- `rules/` - 技术规则文件
- `workflows/` - 流程文档

---

## 常见问题

**Q: Skill 不生效怎么办？**
→ 运行 `bash scripts/install-skill.sh`，然后重启 OpenCode

**Q: 运行报错了怎么办？**
→ 把错误信息直接复制告诉 AI，它会帮你排查和修复

**Q: AI 跳过了某个步骤怎么办？**
→ 直接告诉 AI："你必须按阶段执行，回到阶段 X 重新开始"

**Q: 我想重新做同一个主题怎么办？**
→ 告诉 AI："重新生成 {主题名} 主题"，AI 会覆盖当前阶段的输出

**Q: 配色方案不满意可以改吗？**
→ 可以。在阶段 1 验证时发现问题，告诉 AI 具体要改哪里，修复后再确认
