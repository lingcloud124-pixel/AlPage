// MK Theme index.js
import React from 'react';
import ReactDOM from 'react-dom';

// Theme configuration
const themeColor = "#2C615C";  // Primary color
const sidebarBg = "#F5F7FA";   // Sidebar background
const linkTextColor = "#2C615C"; // Link text color

// Other configurations...
const config = {
  theme: {
    primary: themeColor,
    sidebar: sidebarBg,
    links: linkTextColor
  }
};

export default config;